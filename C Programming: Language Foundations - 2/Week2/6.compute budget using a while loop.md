#include <stdio.h>

int main() {
    int negativeOne , sum = 0 ,inputnumber = 0;
    while (inputnumber != -1)
    {
        scanf("%d" , &inputnumber);
        sum = inputnumber + sum;
        if (inputnumber == -1)
        {
            if (sum <= 0)
            {
                printf("0");
                break;
                
            }
            else if (sum >= 0)
            {
                sum = sum +1 ;
                printf("%d" , sum);
                break;
            }
            
            
        } 
    }
    
    return 0;
}
